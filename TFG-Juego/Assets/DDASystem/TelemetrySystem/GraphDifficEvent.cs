using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class GraphDifficEvent : DDAEvent
{
    public GraphDifficEvent() : base(typeof(GraphDifficEvent).Name)
    {

    }
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
